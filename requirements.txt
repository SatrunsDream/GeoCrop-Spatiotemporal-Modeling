# GeoCrop NAFSI Track 1 — Python Dependencies
# Generated: 2026-04-10
# Install: pip install -r requirements.txt

# ── Core scientific stack ──────────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4

# ── Geospatial ────────────────────────────────────────────────────────────
rasterio>=1.3
geopandas>=0.14
pyproj>=3.6
shapely>=2.0
xarray>=2024.1
rioxarray>=0.15
pyarrow>=15.0       # Parquet export (process_interim_to_parquet, feature tables)
h5py>=3.10          # SMAP HDF5 files
netCDF4>=1.6        # alternative NetCDF access

# ── Remote sensing / time series ──────────────────────────────────────────
pymodis>=2.1        # MODIS data utilities (optional helper)

# ── Machine learning / gradient boosting ─────────────────────────────────
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2       # optional alternative to LightGBM

# ── Visualization ─────────────────────────────────────────────────────────
matplotlib>=3.8
seaborn>=0.13
cartopy>=0.22       # geographic map projections

# ── Notebook environment ──────────────────────────────────────────────────
jupyterlab>=4.1
ipywidgets>=8.1
tqdm>=4.66

# ── Utilities ─────────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
loguru>=0.7
