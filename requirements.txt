# GeoCrop NAFSI Track 1 — Python dependencies
# Install: pip install -r requirements.txt
# On Windows, rasterio/GDAL wheels usually work via pip; if not, install GDAL/rasterio
# from conda-forge first, then pip install the rest of this file.

# ── Core scientific stack ──────────────────────────────────────────────────
# NumPy / Matplotlib: use matched wheels. If you see "compiled using NumPy 1.x
# cannot be run in NumPy 2.x", run: pip install -U "matplotlib>=3.9.2"
# Py3.13+ typically needs NumPy 2 (no stable 1.x wheels from pip).
numpy>=1.26,<2.0; python_version < "3.13"
numpy>=2.0.0,<3.0; python_version >= "3.13"
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3

# ── Arrays, rasters, NetCDF, Parquet ─────────────────────────────────────
xarray>=2024.1
rioxarray>=0.15
rasterio>=1.3
pyarrow>=15.0
h5py>=3.10
netCDF4>=1.6
h5netcdf>=1.3

# ── Geospatial (vector + CRS) ─────────────────────────────────────────────
geopandas>=0.14
pyproj>=3.6
shapely>=2.0

# ── Visualization ─────────────────────────────────────────────────────────
matplotlib>=3.9.2
seaborn>=0.13
cartopy>=0.22

# ── Machine learning ──────────────────────────────────────────────────────
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2
shap>=0.45

# ── Task 4 external features ───────────────────────────────────────────────
# scripts/download_external_features.py uses gridMET (OPeNDAP) + USGS 3DEP REST;
# pydaymet / py3dep / Planetary Computer are not required.

# ── Remote sensing helpers (optional) ─────────────────────────────────────
# pymodis is not imported by this repo’s core code. It depends on the GDAL
# Python bindings, which need native GDAL + gdal-config (e.g. brew install gdal).
# To use it in notebooks: install GDAL first, then: pip install "pymodis>=2.1"

# ── HTTP / downloads (scripts/download_data.py) ───────────────────────────
requests>=2.31

# ── Notebooks & UI ────────────────────────────────────────────────────────
jupyterlab>=4.1
notebook>=7.0
ipykernel>=6.29
ipywidgets>=8.1
tqdm>=4.66

# ── Config & logging ──────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
loguru>=0.7
tabulate>=0.9         # optional: pandas.DataFrame.to_markdown (Task 2 export)

jax
jaxlib
numpyro
multiprocessing
yaml